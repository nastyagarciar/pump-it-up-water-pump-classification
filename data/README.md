# Data

The competition data are **not redistributed** in this public portfolio repository.

To reproduce the workflow, obtain the Pump It Up competition files from the original competition platform and place them here using these names:

- `train_values.csv`
- `train_labels.csv`
- `test_values.csv`

The repository `.gitignore` excludes CSV files in this folder.
